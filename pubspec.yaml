name: losky_factory_app
description: Flutter client scaffold for Losky Factory
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  qr_flutter: ^4.0.0
  uuid: ^3.0.7

flutter:
  uses-material-design: true